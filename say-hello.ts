/**
 * This is a description of the `sayHello` function.
 * 
 * @example
 * // returns 'Hello Alice'
 * `sayHello('Alice')`;
 */
export const sayHello = (name: string) => {
  return `Hello ${name}`;
};
